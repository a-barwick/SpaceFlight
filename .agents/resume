#!/usr/bin/env bash
set -euo pipefail

if [[ ! -x node_modules/.bin/vite ]]; then
  echo "[spaceflight] dependencies are missing or incomplete; run .agents/setup" >&2
  exit 1
fi

echo "[spaceflight] orb dependencies ready"
